# D1

`wrangler.toml.example` is a template for a future Cloudflare Worker.

`worker-example.js` demonstrates read-only querying through a D1 binding.

Do not put Cloudflare API tokens in this directory or in Git.
